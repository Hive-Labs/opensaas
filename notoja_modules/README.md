## Node modules for Notoja

These modules expose a NodeJs API for services to use and connect to other services within the cluster.

To add a module:

  1) Add "require('../notoja_modules/SERVICE_NAME/')"

  2) run ''' npm install -g . ''' from the service directory for which you would like to install a module for.